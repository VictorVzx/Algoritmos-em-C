#include <stdio.h>

int main(void) {
	int a, b, c, soma;

	// Le tres inteiros
	scanf("%d %d %d", &a, &b, &c);

	// Soma os valores lidos
	soma = a + b + c;

	// Exibe a saida
	printf("Saida (soma): %d\n", soma);
	return 0;
}
