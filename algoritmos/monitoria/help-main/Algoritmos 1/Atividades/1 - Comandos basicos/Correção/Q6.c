#include <stdio.h>

int main(void) {
	int a, b, soma;

	// Le dois numeros inteiros
	scanf("%d %d", &a, &b);

	// Calcula a soma
	soma = a + b;

	// Exibe a saida
	printf("Saida (soma): %d\n", soma);
	return 0;
}
